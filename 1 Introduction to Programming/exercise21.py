##
# Computes area of triangle
#

# Read base and height from user
b = float(input("Enter the length of the triangles base: "))
h = float(input("Enter the triangles height: "))

# Calculate area
a = b * h / 2

# Display area
print("The area of the triangle is", "%.3f" % a)
