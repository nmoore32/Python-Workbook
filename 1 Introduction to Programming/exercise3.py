##
# Program asks for length and width of room in feet and computes the area
#

# Read the dimensions from the user
length = float(input("What is the length of the room in feet? "))
width = float(input("What is the width of the room in feet? "))

# Compute the area and display the result
print("The area of the room is", length * width, "square feet.")
